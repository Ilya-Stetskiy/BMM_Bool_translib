#pragma once

#include "core/common.hpp"

namespace bmm {

// bdd_to_tt — полное вычисление таблицы истинности из BDD.
//
// Вход:  Bdd, n_vars() <= kMaxTruthTableVars.
// Выход: TruthTable.
//
// Алгоритм/литература (уточнено во второй итерации задания): материализация
// ОБЯЗАНА идти top-down/диапазонами по структуре BDD, а не точка за точкой
// (см. bmm::Bdd::evaluate() в core/common.hpp — это точечный пример через
// cube-BDD для СЛУЧАЙНОГО доступа к одной точке, здесь же нужна ПОЛНАЯ
// материализация всех 2^n точек, у которой другая оптимальная стратегия).
// Два эквивалентных по духу варианта:
//   - Диапазонное рекурсивное разбиение (аналог `Cudd_ForeachCube` по
//     идее): на узле с переменной x_i — делить текущий диапазон индексов
//     [lo, hi) пополам по биту i (нижняя половина — x_i=0, верхняя —
//     x_i=1), рекурсивно спускаться в hi/lo-потомков с соответствующими
//     под-диапазонами; на терминале — заполнить весь оставшийся
//     под-диапазон одним значением одним махом (memset/fill, а не циклом
//     по отдельным индексам).
//   - Level-synchronous обход по уровням графа (BFS по переменным от
//     корня к терминалам) — эквивалентно по асимптотике, отличается
//     порядком обхода и кэш-локальностью.
//
// !! ЧАСТАЯ ОШИБКА, явно предупреждаем: наивный BOTTOM-UP обход узлов (от
// терминалов к корню, как для bdd_to_anf/bdd_to_aig) НЕ снижает асимптотику
// для ПОЛНОЙ материализации TT — пол всё равно Θ(2^n), таблица истинности
// физически состоит из 2^n бит, меньше в принципе не бывает. Хуже того:
// попытка "мемоизировать локальный кусок таблицы на узел" (т.е. хранить у
// каждого BDD-узла материализованную под-таблицу его под-функции и
// комбинировать снизу вверх) может УХУДШИТЬ работу до Θ(n·2^n) — на каждом
// из n уровней графа копируется/комбинируется таблица размера Θ(2^n),
// вместо того чтобы просто один раз записать каждый бит финальной таблицы.
// Мемоизация полезна для bdd_to_anf/bdd_to_aig (там результат на узле —
// компактная структура: ANF-полином/AIG-сигнал, а не 2^(n-глубина) бит),
// здесь — нет: top-down разбиение диапазонов без сохранения промежуточных
// таблиц per-node — единственный вариант, не проигрывающий асимптотически.
//
// Параллелизм: core/CONVENTIONS.md п.6, правило 1 берёт верх над правилом
// 3 (несмотря на то, что выход — плоский TT): Bdd — источник, поэтому все
// операции чтения BDD обязаны идти через Sylvan API. НЕ заводите
// `#pragma omp parallel for` с необслуживаемыми Lace потоками, вызывающими
// Sylvan напрямую — это может привести к порче внутреннего состояния
// Sylvan. Если нужен параллелизм по точкам, используйте механизмы,
// совместимые с Lace (например, `sylvan::run` / внутренние Lace-таски), а
// не сырой OpenMP.
//
// Тесты: test_bdd.cpp, секция "bdd_to_tt".
Result<TruthTable> bdd_to_tt(const Bdd& bdd);

}  // namespace bmm
