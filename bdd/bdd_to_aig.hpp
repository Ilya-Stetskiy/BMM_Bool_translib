#pragma once

#include "core/common.hpp"

namespace bmm {

// bdd_to_aig — построение AIG, вычисляющего ту же функцию, что и BDD.
//
// Вход:  Bdd (sylvan::Bdd, n_vars() задан явно в обёртке — см.
//        core/common.hpp).
// Выход: Aig (mockturtle::aig_network, один PO).
//
// Алгоритм/литература (уточнено во второй итерации задания): есть ДВА
// известных маршрута, используйте маршрут (а).
//
// (а) — используемый маршрут — прямой структурный обход: каждый внутренний
// узел BDD с переменной x_i и потомками hi/lo -> ITE(x_i, hi_aig, lo_aig)
// -> до 3 AND-узлов через де Моргана (MUX(s,hi,lo) =
// NOT(AND(NOT(AND(s,hi)), NOT(AND(NOT(s),lo))))) — тот же приём, что
// использует verify/sat_encoding/sat_encoding.cpp::reference_aig_from_table
// для независимого эталонного AIG (НЕ вызывайте её отсюда, это тестовый
// код). Листья BDD (терминалы 0/1) -> aig.get_constant(false/true).
// Сложность O(|BDD|), верхняя оценка размера результата — 3·|BDD| AND-узлов.
// Мемоизация — по КАНОНИЧЕСКОМУ (без учёта complement-бита на входящем
// ребре) указателю/хэндлу узла: Sylvan, как и CUDD, использует
// комплементарные рёбра, поэтому для узла нужно отдельно учитывать (1) его
// "regular"-форму для ключа мемоизации (аналог `Cudd_Regular` в
// CUDD-терминологии; в низкоуровневом C API Sylvan — `sylvan_regular`; если
// работаете через `sylvan::Bdd`/`sylvan_obj.hpp`, проверьте, какие
// accessor'ы для low/high-узлов и статуса complement-бита он предоставляет
// в вашей версии, при необходимости используйте низкоуровневый
// `sylvan_low`/`sylvan_high`/`sylvan_var` из `sylvan.h`) и (2) сам факт
// complement (аналог `Cudd_IsComplement`) — при complement на входящем
// ребре результат соответствующей ветки нужно инвертировать (`!signal` у
// mockturtle) уже ПОСЛЕ того, как взяли из кэша мемоизации результат для
// канонического узла, а не заводить отдельный кэш на каждую полярность
// (иначе мемоизация вырождается и вы теряете весь выигрыш от неё).
// Готовой библиотечной функции для этого пути нет ни в CUDD, ни в ABC, ни в
// mockturtle — пишется вручную.
//
// (б) — НЕ используйте как основной путь — через SOP (`collapse`→`sop`→
// `strash` в ABC, `convert_cover_to_graph` в mockturtle): минимальный SOP
// доказуемо НЕ ограничен размером исходного BDD (Sasao & Butler, "The
// Minimization of Multiple-Valued Input Two-Valued Output Functions" /
// смежные результаты по SOP-vs-BDD размерам — возможен неограниченный рост
// в худшем случае, в отличие от гарантированной верхней оценки 3·|BDD| у
// маршрута (а)).
//
// Важно про порядок переменных: маршрут (а) НАСЛЕДУЕТ порядок переменных
// исходного BDD — раздутый (неоптимальный) порядок переменных даёт раздутый
// AIG, и структурное хеширование (мемоизация по узлу) это НЕ лечит — оно
// убирает дублирование одинаковых поддеревьев, а не размер, порождённый
// самим порядком. Если на входе заведомо плохой порядок — это ограничение
// метода, а не баг реализации.
//
// Реализовать ИТЕРАТИВНО (явный стек/очередь узлов в топологическом
// порядке от терминалов к корню), НЕ РЕКУРСИЕЙ — на глубоких BDD (много
// переменных, длинные цепочки) рекурсивный обход рискует переполнить стек
// вызовов; итеративный обход с явной структурой данных на куче этого риска
// не имеет.
//
// Graceful degradation (core/CONVENTIONS.md п.2а): это ОДНА ИЗ функций, для
// которых обязателен паттерн "оберните тело в try/catch(std::bad_alloc&),
// верните ErrorCode::OutOfMemory" — раздутый порядок переменных BDD (см.
// выше) может дать неограниченно большой AIG, и это единственный способ
// узнать о проблеме заранее по размеру входа.
//
// Параллелизм: core/CONVENTIONS.md п.6, правило 1 — Bdd является входом,
// поэтому обход BDD-узлов делайте только через Sylvan API, без
// собственного TBB/OpenMP вокруг вызовов в Sylvan; сборку AIG (создание
// gate'ов через mockturtle) стройте последовательно по мере обхода.
//
// Тесты: test_bdd.cpp, секция "bdd_to_aig".
Result<Aig> bdd_to_aig(const Bdd& bdd);

}  // namespace bmm
