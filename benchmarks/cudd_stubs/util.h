// Пустая заглушка для CUDD-пакетного `util.h`.
//
// `/usr/local/include/dddmp.h` в этом Docker-образе делает `#include
// "util.h"` (наследие исходного дерева CUDD, где util.h — отдельный пакет
// с портируемостью-макросами) — но при установке в этот образ САМ util.h
// не попал в /usr/local/include (только cudd.h/dddmp.h, скомпилированные
// в libcudd.a). Проверено эмпирически: ни dddmp.h, ни cudd.h НЕ используют
// напрямую ни одного идентификатора из util.h (не найдено ни EXTERN, ни
// util_cpu_time/MMalloc/MFree и т.п. через grep по обоим заголовкам) — сам
// #include, судя по всему, вестигиальный (перешёл по конвенции пакетной
// структуры CUDD, не по фактической необходимости). Пустой файл достаточен,
// чтобы преодолеть этот #include, не подделывая функциональность, которая
// не используется.
//
// Включается ТОЛЬКО для целей, которые собирают мост DDDMP->Sylvan (см.
// target_include_directories в CMakeLists.txt, добавлен ПЕРЕД системным
// путём, где лежит настоящий dddmp.h) — не влияет ни на что, что реально
// линкует BRiAl (тот использует CUDD ВНУТРИ себя, через собственные bundled
// заголовки polybori/cudd/, а не через /usr/local/include/cudd.h+dddmp.h).
#pragma once
