#pragma once

#include "core/common.hpp"

namespace bmm {

// tt_to_anf — построение полинома Жегалкина (ANF) из таблицы истинности.
//
// Вход:  TruthTable, n_vars() <= kMaxTruthTableVars.
// Выход: Anf.
//
// Алгоритм/литература: быстрое преобразование Мёбиуса/Жегалкина (fast
// zeta/mobius transform над GF(2)) — классический результат (восходит к
// Жегалкину, 1927; современное изложение — Carlet, "Boolean Functions for
// Cryptography...", 2010). В идейном виде, in-place по массиву коэффициентов
// a[0..2^n-1] (изначально равному таблице истинности): для каждого i в
// 0..n-1, для каждого x с битом i выставленным — a[x] ^= a[x без бита i].
// Независимая эталонная реализация того же алгоритма есть в
// verify/metamorphic/metamorphic.cpp::mobius_transform (используется ТОЛЬКО
// как метаморфный инвариант в тестах, не вызывайте её из production-кода —
// иначе тест перестанет быть независимым от реализации).
// a[mask]==1 после преобразования означает, что моном
// prod_{i: mask бит i=1} x_i входит в полином с коэффициентом 1.
// Итоговое количество мономов может быть экспоненциальным по n — это
// свойство самой функции (например, для "случайной" функции почти все 2^n
// коэффициентов ненулевые), а не проблема алгоритма.
//
// Параллелизм: core/CONVENTIONS.md п.6 — формально правило 2 (anf/* -> TBB)
// применимо к папке целиком, но сам transform естественно ложится на
// плоский массив (butterfly-паттерн, как в FFT) — см. заметку-исключение в
// конце CONVENTIONS.md п.6: OpenMP для самого transform, TBB
// (task_group + concurrent_hash_map) для последующей сборки результата в
// граф/ZDD-структуру Anf (BooleSet при BMM_HAVE_BRIAL, либо
// std::set<Monomial> в AnfFallback) — выбор конкретной разбивки за вами.
//
// Обязательный бенчмарк (вторая итерация задания): формально TBB-функция
// (см. "Параллелизм" выше) — реализация ДОЛЖНА пройти секцию
// "tt_to_anf_tbb_scaling" в test_anf.cpp (замер 1-поточного vs полного
// tbb::global_control на n=8/12/16, см. benchmarks/tbb_scaling.hpp).
// Учитывая заметку-исключение выше (transform естественно ложится на
// OpenMP) — если вы распараллелили именно эту часть через OpenMP, а не
// TBB, `tbb::global_control` не повлияет на неё, и speedup в этом
// бенчмарке будет отражать только TBB-часть (сборку графа/ZDD) — это
// ожидаемо, задокументируйте в самом выводе бенчмарка, какая часть чем
// распараллелена, если разбивка нетривиальна.
//
// Тесты: test_anf.cpp, секции "tt_to_anf" (корректность) и
// "tt_to_anf_tbb_scaling" (обязательный бенчмарк параллельности).
Result<Anf> tt_to_anf(const TruthTable& tt);

}  // namespace bmm
