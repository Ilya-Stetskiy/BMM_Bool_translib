#pragma once

#include "core/anf_repr.hpp"

namespace bmm {

// anf_to_tt — вычисление таблицы истинности из полинома Жегалкина (ANF).
//
// Вход:  Anf.
// Выход: TruthTable, n_vars() <= kMaxTruthTableVars.
//
// Алгоритм/литература: приятный факт — преобразование Мёбиуса/Жегалкина над
// GF(2) является инволюцией (применение его дважды к одному и тому же
// массиву возвращает исходный массив). Значит anf_to_tt — это БУКВАЛЬНО тот
// же алгоритм, что и tt_to_anf (anf/tt_to_anf.hpp), применённый в обратную
// сторону: a[mask] интерпретируется как коэффициент монома, преобразование
// восстанавливает значения функции в каждой точке. Реализуйте одну общую
// внутреннюю функцию mobius_transform_inplace(a[], n) и вызовите её из
// обеих tt_to_anf/anf_to_tt (независимая копия для тестов уже есть в
// verify/metamorphic/metamorphic.cpp::mobius_transform — не вызывайте её
// из production-кода, см. пояснение в tt_to_anf.hpp).
//
// Параллелизм: core/CONVENTIONS.md п.6, правило 3 — выход TT, плоский
// массив -> OpenMP `#pragma omp parallel for` по внешнему циклу
// преобразования (по битовой позиции i это последовательно, но внутренний
// цикл по mask в 0..2^n-1 для фиксированного i независим по итерациям и
// хорошо параллелится).
//
// Тесты: test_anf.cpp, секция "anf_to_tt".
Result<TruthTable> anf_to_tt(const Anf& anf);

}  // namespace bmm
