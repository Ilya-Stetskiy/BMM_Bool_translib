#pragma once

#include "core/common.hpp"

namespace bmm {

// thr_to_aig — построение AIG, вычисляющего заданную пороговую функцию.
//
// Вход:  Thr (веса w_1..w_n, порог theta).
// Выход: Aig (mockturtle::aig_network, один PO).
//
// !! ЯВНО: НЕ ИДИТЕ ЧЕРЕЗ BDD (не thr_to_bdd + bdd_to_aig). Это не
// оптимизация "на будущее" — thr_to_bdd размер BDD НЕ гарантированно
// полиномиален для произвольных весов (см. предупреждение в
// thr/thr_to_bdd.hpp), а bdd_to_aig в худшем случае наследует этот размер.
// Путь напрямую из (w, theta) ниже даёт гарантированно полиномиальный
// результат независимо от конкретных весов — теряя этот путь через BDD,
// вы теряете саму гарантию.
//
// Алгоритм/литература: Eén, N., Sörensson, N., "Translating Pseudo-Boolean
// Constraints into SAT", JSAT, 2006 — кодирование псевдобулева ограничения
// (sum w_i*x_i >= theta) в CNF/схему через сортирующие сети или
// adder-based кодирование (для положительных весов часто удобна схема на
// базе тотализатора/сортирующей сети над unary-представлением; для
// произвольных знаков весов — сначала привести к неотрицательным весам
// заменой x_i -> (1-x_i) там, где w_i < 0, скорректировав theta). Строится
// НАПРЯМУЮ из (w, theta): дерево сумматоров (adder tree) для Σw_i*x_i +
// компаратор результата с theta, каждый элемент (полный/полу-сумматор,
// компаратор) расписан через AND+инвертор поэлементно (та же техника
// De Morgan, что и в anf/anf_to_aig.hpp для XOR: полный сумматор — это два
// XOR + два AND на бит, компаратор — цепочка сравнений по битам от
// старшего к младшему). Размер O(n·B), где B — битовая ширина весов
// (разрядность промежуточных сумм) — ГАРАНТИРОВАННО полиномиально по входу,
// в отличие от пути через BDD (см. предупреждение выше). Применяйте
// STRUCTURAL HASHING сразу при построении (переиспользование одинаковых
// AND-узлов через `mockturtle::aig_network::create_and`, которая делает
// это по умолчанию) — не постройте сначала наивное дерево, а потом
// пытайтесь дедуплицировать постфактум.
//
// Параллелизм: core/CONVENTIONS.md п.6, правило 2 — Thr не является Bdd,
// а построение AIG — это построение графа, поэтому правило "aig/anf ->
// TBB" применимо к самой операции построения (даже притом что папка —
// thr/): TBB task_group для независимых веток сортирующей сети/сумматора +
// concurrent_hash_map для мемоизации.
//
// Тесты: test_thr.cpp, секция "thr_to_aig".
Result<Aig> thr_to_aig(const Thr& thr);

}  // namespace bmm
