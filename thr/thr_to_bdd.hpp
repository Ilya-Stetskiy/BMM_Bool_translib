#pragma once

#include "core/common.hpp"

namespace bmm {

// thr_to_bdd — построение BDD, вычисляющего заданную пороговую функцию.
//
// Вход:  Thr (веса w_1..w_n, порог theta).
// Выход: Bdd (sylvan::Bdd).
//
// Алгоритм/литература: Behle, M., "Binary Decision Diagrams and Integer
// Programming" (диссертация/статьи 2007–2008) и Hosaka, K. et al., "Size of
// Ordered Binary Decision Diagrams Representing Threshold Functions", 1997
// — оба описывают эффективное построение BDD для пороговой функции через
// рекурсивное разбиение по частичным суммам: узел на уровне i хранит
// достижимый диапазон частичной суммы sum_{j<i} w_j*x_j, и рекурсия идёт по
// оставшемуся "бюджету" до порога — классический приём для BDD-
// представления псевдобулевых ограничений (state = текущая частичная
// сумма, а не полное присвоение), что даёт существенно компактнее дерево,
// чем наивное Shannon-разложение по всем x_i независимо от значений весов.
// Строить РЕКУРСИЕЙ С МЕМОИЗАЦИЕЙ по паре (индекс переменной, остаточный
// порог) — ключ мемоизации именно эта пара, не индекс переменной один, т.к.
// разные "остаточные бюджеты" на одном уровне дают разные под-BDD.
//
// !! ПРЕДУПРЕЖДЕНИЕ (уточнено во второй итерации задания): размер BDD
// ПОЛИНОМИАЛЕН НЕ ДЛЯ ВСЕХ пороговых функций — это зависит от весов, не
// только от n. Для унарных весов (все w_i равны — функции-счётчики/majority-
// подобные) — O(n²) узлов, компактно. Для произвольных целых весов при
// ЭКСПОНЕНЦИАЛЬНО РАЗНЕСЁННЫХ значениях (например w_i = 2^i) BDD может быть
// экспоненциальным даже при оптимальном порядке переменных — число различимых
// достижимых частичных сумм само растёт экспоненциально с числом
// переменных при таких весах, и разбиение по частичным суммам выше это не
// обходит, оно от этого напрямую зависит. Это (предположительно) связано с
// результатом Håstad, J., 1994, о нижних оценках размера BDD/формул для
// функций с экспоненциально растущими весами — **точную формулировку и
// применимость к этому конкретному случаю пометьте как "требует
// перепроверки"** (то есть не берите это утверждение как точную цитату без
// сверки с первоисточником — общая идея "экспоненциально разнесённые веса
// -> потенциально экспоненциальный BDD" достаточно устоявшаяся в теории
// пороговой логики, но конкретная атрибуция результата 1994 года к этой
// формулировке не проверена в рамках подготовки этого скелета).
//
// Параллелизм: core/CONVENTIONS.md п.6, правило 1 — Bdd является выходом,
// значит только Sylvan/Lace (Ite/операторы), никакого собственного
// TBB/OpenMP, даже для параллельного построения узлов на разных уровнях
// частичных сумм.
//
// Тесты: test_thr.cpp, секция "thr_to_bdd".
Result<Bdd> thr_to_bdd(const Thr& thr);

}  // namespace bmm
