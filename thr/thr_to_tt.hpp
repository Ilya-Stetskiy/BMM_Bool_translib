#pragma once

#include "core/common.hpp"

namespace bmm {

// thr_to_tt — вычисление таблицы истинности для заданной пороговой
// функции.
//
// Вход:  Thr (веса w_1..w_n, порог theta), n <= kMaxTruthTableVars.
// Выход: TruthTable.
//
// Алгоритм/литература: тривиальная прямая оценка линейной формы в каждой
// из 2^n точек: f(x) = [sum(w_i*x_i) >= theta]. Единственная тонкость —
// эффективно проходить по точкам (например, используя код Грея, чтобы
// пересчитывать частичную сумму инкрементально при переходе между соседними
// точками вместо полного пересчёта — необязательная оптимизация).
//
// Параллелизм: core/CONVENTIONS.md п.6, правило 3 — OpenMP `#pragma omp
// parallel for` по индексу точки 0..2^n-1 над плоским `uint64_t[]`/
// `bool[]`-буфером (без объектов/указателей в теле цикла — задел под
// перенос на GPU, см. profiling/README.md). Это самая простая из 20
// функций проекта — хороший кандидат для первой реализации, чтобы
// освоиться с контрактом Result<T>/test_runner.hpp.
//
// Тесты: test_thr.cpp, секция "thr_to_tt".
Result<TruthTable> thr_to_tt(const Thr& thr);

}  // namespace bmm
